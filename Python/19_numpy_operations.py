import numpy as np 

numbers = np.array([10,20,30,40,50])
print(numbers)


# to find mean / average
print(numbers.mean())

# to find median / middle value
print(np.median(numbers))

# to find satndard deviation / how much the data is spread out
print(numbers.std())